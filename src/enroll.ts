class EnrollPlugin extends WebRegPlugin {
  static isEnrollPage(win: Window, doc: HTMLDocument): boolean {
    if (!win.location.href.includes("wramia")) {
      return false;
    }
    doc.
    return false;
  }
}
